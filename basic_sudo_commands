sudo apt update -to update package list
sudo apt upgrade - to upgrade all installed packages
sudo apt install - to install a pakage
sudo apt remove -remove package_name
sudo apt search - searches for a package in repositories
sudo chmod - changess permissions of a file
sudo chown - changes the owner and group of a file
sudo reboot - restarts computer
